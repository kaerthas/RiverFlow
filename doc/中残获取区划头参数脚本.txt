 def result =  new JSONObject()

 def AppKey  = JSONObject.parseObject(args).get("AppKey")

 def rid = JSONObject.parseObject(args).get("rid")
 def sid = JSONObject.parseObject(args).get("sid")
 def appsecret  = JSONObject.parseObject(args).get("AppSecret")

 def timestamp =  System.currentTimeMillis()
 def sign = AESUtils.getSign(sid, rid,timestamp+"",appsecret);


 result.put("AppKey",AppKey)
 result.put("rid",rid)
 result.put("sid",sid)
 result.put("Timestamp",timestamp)
 result.put("sign",sign)
 return result